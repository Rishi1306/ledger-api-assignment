apiVersion: v1
kind: Namespace
metadata:
  name: payments
  labels:
    # Pod Security Standards: "restricted" is the strictest built-in policy.
    # enforce = reject pods that violate it at creation time.
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/enforce-version: latest
